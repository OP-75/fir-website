// const server_url = `http://localhost:5000` //for devlopment
const server_url = `https://fir-website-backend.vercel.app` // for deployment on vercel, besure to uncomment this and comment above url before pushing

export default server_url;